import "@/style.scss";

const app = document.querySelector<HTMLDivElement>("#app");
if (app)
  app.innerHTML = `
  <h1 class="text-5xl font-bold">Vite + TS + SASS + Tailwind + ESLint + Prettier + TS-Jest</h1> 
`;
