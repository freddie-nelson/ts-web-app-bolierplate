import "./style.css";

const app = document.querySelector<HTMLDivElement>("#app");
if (app)
  app.innerHTML = `
  <h1 class="text-5xl font-bold">Vite + TS + Tailwind + ESLint + Prettier + TS-Jest</h1> 
`;
